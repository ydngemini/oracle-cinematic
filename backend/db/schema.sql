-- ===========================================================================
-- Oracle — Multi-Tenant IAM + RLS
--
-- This prototype has been promoted to versioned migrations. Apply in order:
--   backend/db/migrations/0001_init_tenancy.sql   — tenants/users/clients/
--       leads/listings, helper fns, RLS, oracle_app role
--   backend/db/migrations/0002_listing_grants.sql — explicit co-broke grant
--       join table + grant-aware listings read policy
--
-- The Python enforcement mirror lives in backend/tenancy.py; the Auth
-- Gatekeeper (backend/auth.py) stamps tenant_id + role into the JWT.
--
-- Do not edit this file — it is kept only as a pointer. The migrations are the
-- single source of truth (decision 006, Hybrid Tenant Isolation).
-- ===========================================================================
