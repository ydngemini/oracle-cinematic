{
  "// note": "Ceilings, not targets. Lower deliberately; raise only with a stated reason.",
  "// raised": "2026-08-22, three small raises in one session, none adding a dependency: (1) +2.2 KB raw / +1.0 KB gzip \u2014 tour affordance on Property View (src/lib/tour/tourOffer.ts), measured against a reconstructed pre-change baseline that sat exactly at the old ceiling; (2) +0.8 KB gzip \u2014 360\u00b0 capture mode (mode toggle, equirect requirement, floor selector); (3) +0.2 KB raw \u2014 form_id and obligation on the state document checklist, so two documents required by one rule stay distinguishable. Viewers remain lazy: TourViewer/PanoViewer/WalkableSplatViewer are still separate chunks, so none of this is viewer weight entering the main bundle.",
  "// raised-2026-08-26": "Backend-parity sweep: +121.5 KB raw / +39.3 KB gzip in total, adding NO dependency. Every byte is either code already in this repository that no user could reach, or a new panel over an endpoint that shipped with no caller. Phase A (+94.7 KB): mounted fifteen orphaned components \u2014 DealPipeline (+PipelineBoard, LeadMap, DossierPanel), PortfolioTab, ContractDraftWorkspace, DealIntakePanel, ClientTaskList, StateDocumentLibrary, MarketDataPanels, LiveTranscript, LegalMatrix. DealIntakePanel is the only screen that can create a transaction, so deal creation was impossible in-product. Phases B/C (+26.8 KB): DealRoomPanel (parties, milestones), ComplianceChecklistPanel (its GET half reported total_items:0 on every transaction because nothing called the write half), PropertyIntelligencePanel, PublicRecordsDiligence, ContactIntakePanel (POST /api/crm/contacts had no caller, so People could list the book and never add to it), SitePublishPanel (nine sites_api routes: a site could be drafted and previewed but never published, shared, or measured), model activate/rollback, and three AdminOps panels. largestChunkBytes is UNCHANGED at 2325752 \u2014 that chunk is PlayCanvas, and it not moving is the check that all of this landed as lazy route chunks rather than main-bundle weight.",
  "// largest": "src-CvflWdIz.js",
  "largestChunkBytes": 2325752,
  "largestChunkGzipBytes": 591590,
  "totalBytes": 3883055,
  "totalGzipBytes": 1075361
}
